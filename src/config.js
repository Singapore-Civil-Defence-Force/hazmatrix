// File for all global configuration values

const config = {
  // Base URL of app, change this if hosting URL changes
  // Base URL does include the #/ which is the base URL for the SPA router too
  baseURL: "https://singapore-civil-defence-force.github.io/hazmatrix/#/",
};

export default config;
